# ── Debug mode ─────────────────────────────────────────────────────────────────
# Set to 1 to enable verbose DEBUG-level logging across all src.* modules.
# Leave as 0 for normal INFO-level output.
DEBUG=0

# ── Random seed ─────────────────────────────────────────────────────────────────
# Controls reproducibility for model training and bootstrap simulations.
# The default (1123) is set in src/utils/config.py — uncomment to override.
# RANDOM_SEED=1123
